# Mail Catcher Service Configuration
# This is included in docker-compose.yml when mail_catcher is enabled

# MailHog Service (already in docker-compose.yml)
# mailhog:
#   image: mailhog/mailhog:latest
#   container_name: ${PROJECT_NAME}-mailhog
#   ports:
#     - "1025:1025"  # SMTP
#     - "8025:8025"  # Web UI
#   networks:
#     - ${PROJECT_NAME}-network

# Alternative: Mailpit
# mailpit:
#   image: axllent/mailpit:latest
#   container_name: ${PROJECT_NAME}-mailpit
#   ports:
#     - "1025:1025"  # SMTP
#     - "8025:8025"  # Web UI
#   networks:
#     - ${PROJECT_NAME}-network

# Laravel .env configuration for MailHog:
# MAIL_MAILER=smtp
# MAIL_HOST=mailhog
# MAIL_PORT=1025
# MAIL_USERNAME=null
# MAIL_PASSWORD=null
# MAIL_ENCRYPTION=null
# MAIL_FROM_ADDRESS="noreply@example.com"
# MAIL_FROM_NAME="${APP_NAME}"
